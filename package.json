{
  "name": "whats-today-backend",
  "version": "0.0.1",
  "private": true,
  "description": "What's Today — AI가 매일 선정하는 시사 이슈 토론 포럼 (백엔드)",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
