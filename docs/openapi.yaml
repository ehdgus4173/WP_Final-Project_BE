openapi: 3.0.3
info:
  title: What's Today — Backend API
  version: "1.0.0"
  description: >
    AI가 매일 선정·관리자가 검수·게시한 단 하나의 시사 이슈에 대해 토론하는 포럼의 백엔드 API.
    모든 응답은 공통 envelope을 사용한다: 성공 `{ success: true, data }`, 실패 `{ success: false, error: { code, message } }`.
    인증은 JWT(HS256)를 `Authorization: Bearer <token>` 헤더로 전달한다.

servers:
  - url: http://localhost:3000/api
    description: Local
  - url: https://wp-final-projcet-be.onrender.com/api
    description: Render (production)

tags:
  - name: Health
  - name: Auth
  - name: Home
  - name: Issue
  - name: Posts
  - name: Comments
  - name: Admin
  - name: Cron

paths:
  /health:
    get:
      tags: [Health]
      summary: 프로세스 liveness
      responses:
        "200":
          description: up
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          status: { type: string, example: up }
                          uptime: { type: number, example: 123.45 }

  /health/db:
    get:
      tags: [Health]
      summary: DB 연결 확인 (SELECT 1)
      responses:
        "200":
          description: DB up
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          db: { type: string, example: up }
        "503":
          description: DB 연결 실패
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /auth/register:
    post:
      tags: [Auth]
      summary: 회원가입 (role 입력 무시 — 항상 'user')
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, username, password]
              properties:
                email: { type: string, format: email, example: user@example.com }
                username: { type: string, example: alex_92, description: "3~20자 영문·숫자·_" }
                password: { type: string, format: password, example: abc12345, description: "최소 8자, 영문+숫자" }
      responses:
        "201":
          description: 생성됨
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          user: { $ref: "#/components/schemas/User" }
        "400": { $ref: "#/components/responses/BadInput" }
        "409":
          description: 이메일/유저네임 중복 (EMAIL_TAKEN / USERNAME_TAKEN)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /auth/login:
    post:
      tags: [Auth]
      summary: 로그인 (응답 body에 JWT token)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email: { type: string, format: email }
                password: { type: string, format: password }
      responses:
        "200":
          description: 로그인 성공
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          token: { type: string }
                          user: { $ref: "#/components/schemas/User" }
        "400": { $ref: "#/components/responses/BadInput" }
        "401":
          description: 자격 불일치 (INVALID_CREDENTIALS)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "429":
          description: Rate limit 초과 (15분당 5회)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /auth/me:
    get:
      tags: [Auth]
      summary: 내 정보 조회
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: 내 정보
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          user: { $ref: "#/components/schemas/User" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /home:
    get:
      tags: [Home]
      summary: 오늘 이슈 1건 + 과거 이슈 최근 10건 (published만)
      responses:
        "200":
          description: 홈 데이터
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          today_issue:
                            nullable: true
                            allOf: [{ $ref: "#/components/schemas/IssueCard" }]
                          past_issues:
                            type: array
                            items: { $ref: "#/components/schemas/IssueCard" }

  /issues/{id}:
    get:
      tags: [Issue]
      summary: 이슈(published) + 게시물 목록
      parameters:
        - $ref: "#/components/parameters/IssueId"
        - in: query
          name: sort
          schema: { type: string, enum: [top, latest], default: top }
      responses:
        "200":
          description: 이슈 + 게시물 목록
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          issue: { $ref: "#/components/schemas/IssueCard" }
                          posts:
                            type: array
                            items: { $ref: "#/components/schemas/PostCard" }
        "400": { $ref: "#/components/responses/BadInput" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/issues:
    get:
      tags: [Admin]
      summary: 이슈 검수 목록 (관리자)
      security: [{ bearerAuth: [] }]
      parameters:
        - in: query
          name: status
          schema: { type: string, enum: [pending, published], default: pending }
      responses:
        "200":
          description: 이슈 목록
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          issues:
                            type: array
                            items: { $ref: "#/components/schemas/Issue" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /admin/issues/{id}:
    patch:
      tags: [Admin]
      summary: 이슈 승인 (pending → published)
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/IssueId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [published] }
      responses:
        "200":
          description: 승인됨
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          id: { type: string, example: "130" }
                          status: { type: string, example: published }
                          published_at: { type: string, format: date-time }
        "400": { $ref: "#/components/responses/BadInput" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: 이미 게시됨 (ALREADY_PUBLISHED)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    delete:
      tags: [Admin]
      summary: 이슈 거절 (pending hard delete)
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/IssueId"
      responses:
        "204": { description: 삭제됨 (body 없음) }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /cron/generate-issues:
    post:
      tags: [Cron]
      summary: "[internal] 오늘의 이슈 생성 (Gemini → pending). pg_cron이 호출."
      description: "Authorization 헤더에 CRON_SECRET을 Bearer로 전달. 일반 JWT 아님."
      security: [{ cronSecret: [] }]
      responses:
        "200":
          description: 생성/스킵
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          date: { type: string, example: "2026-06-07" }
                          status: { type: string, enum: [success, skipped] }
                          issue_id: { type: string, example: "131" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "500":
          description: Gemini 호출/파싱 실패
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /issues/{id}/posts:
    post:
      tags: [Posts]
      summary: 게시물 작성
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/IssueId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, content]
              properties:
                title: { type: string, maxLength: 120 }
                content: { type: string, minLength: 20, maxLength: 10000, description: 마크다운 원문 }
      responses:
        "201":
          description: 생성됨
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          id: { type: string, example: "5021" }
                          issue_id: { type: string, example: "100" }
                          title: { type: string }
                          created_at: { type: string, format: date-time }
        "400": { $ref: "#/components/responses/BadInput" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /posts/{id}:
    get:
      tags: [Posts]
      summary: 게시물 상세 (본문·점수·user_vote + 댓글·대댓글)
      description: optionalAuth — 로그인 시 user_vote / 각 댓글 liked_by_me 채워짐.
      parameters:
        - $ref: "#/components/parameters/PostId"
      responses:
        "200":
          description: 게시물 상세
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          post: { $ref: "#/components/schemas/PostDetail" }
                          comments:
                            type: array
                            items: { $ref: "#/components/schemas/Comment" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [Posts]
      summary: 게시물 수정 (작성자 본인만 — admin도 불가)
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PostId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, content]
              properties:
                title: { type: string, maxLength: 120 }
                content: { type: string, minLength: 20, maxLength: 10000 }
      responses:
        "200":
          description: 수정됨
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data:
                        type: object
                        properties:
                          id: { type: string }
                          title: { type: string }
                          updated_at: { type: string, format: date-time }
        "400": { $ref: "#/components/responses/BadInput" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [Posts]
      summary: 게시물 삭제 (작성자 또는 admin)
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PostId"
      responses:
        "204": { description: 삭제됨 (body 없음) }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /posts/{id}/votes:
    post:
      tags: [Posts]
      summary: 추천/비추천 토글
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PostId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [value]
              properties:
                value: { type: integer, enum: [1, -1] }
      responses:
        "201":
          description: 신규 추천 (created)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data: { $ref: "#/components/schemas/VoteResult" }
        "200":
          description: 변경 또는 취소 (updated | deleted)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data: { $ref: "#/components/schemas/VoteResult" }
        "400": { $ref: "#/components/responses/BadInput" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /posts/{id}/comments:
    post:
      tags: [Comments]
      summary: 댓글·대댓글 작성 (parent_id 있으면 대댓글)
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PostId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content: { type: string, minLength: 2, maxLength: 1000 }
                parent_id: { type: integer, nullable: true, description: "있으면 대댓글(부모는 depth 0)" }
      responses:
        "201":
          description: 생성됨
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data: { $ref: "#/components/schemas/Comment" }
        "400":
          description: 입력 오류 또는 대댓글에 답글 (BAD_INPUT / CANNOT_REPLY_TO_REPLY)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404":
          description: 게시물/부모 댓글 없음 (POST_NOT_FOUND / PARENT_NOT_FOUND)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /comments/{id}:
    delete:
      tags: [Comments]
      summary: 댓글·대댓글 삭제 (작성자 또는 admin)
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CommentId"
      responses:
        "204": { description: 삭제됨 (body 없음) }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /comments/{id}/likes:
    post:
      tags: [Comments]
      summary: 댓글·대댓글 좋아요 토글
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CommentId"
      responses:
        "201":
          description: 좋아요 추가 (created)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data: { $ref: "#/components/schemas/LikeResult" }
        "200":
          description: 좋아요 취소 (deleted)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Success"
                  - properties:
                      data: { $ref: "#/components/schemas/LikeResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    cronSecret:
      type: http
      scheme: bearer
      description: "CRON_SECRET (pg_cron 전용)"

  parameters:
    IssueId:
      in: path
      name: id
      required: true
      schema: { type: integer }
      description: Issue ID
    PostId:
      in: path
      name: id
      required: true
      schema: { type: integer }
      description: Post ID
    CommentId:
      in: path
      name: id
      required: true
      schema: { type: integer }
      description: Comment ID

  schemas:
    Success:
      type: object
      properties:
        success: { type: boolean, example: true }
        data: { type: object }
    Error:
      type: object
      properties:
        success: { type: boolean, example: false }
        error:
          type: object
          properties:
            code: { type: string, example: BAD_INPUT }
            message: { type: string }
            details: { type: array, items: { type: object } }
    User:
      type: object
      properties:
        id: { type: string, example: "42" }
        email: { type: string, example: user@example.com }
        username: { type: string, example: alex_92 }
        role: { type: string, enum: [user, admin], example: user }
        created_at: { type: string, format: date-time }
    Issue:
      type: object
      properties:
        id: { type: string, example: "130" }
        title: { type: string }
        summary: { type: string, nullable: true }
        source_url: { type: string, nullable: true }
        status: { type: string, enum: [pending, published] }
        published_at: { type: string, format: date-time, nullable: true }
        created_at: { type: string, format: date-time }
    IssueCard:
      type: object
      properties:
        id: { type: string, example: "100" }
        title: { type: string }
        summary: { type: string, nullable: true }
        source_url: { type: string, nullable: true, description: "today_issue에만 포함" }
        date: { type: string, example: "2026-06-07", description: "KST 날짜" }
        post_count: { type: integer, example: 12 }
        created_at: { type: string, format: date-time }
    PostCard:
      type: object
      properties:
        id: { type: string, example: "5021" }
        title: { type: string }
        body_preview: { type: string, description: "본문 첫 100자" }
        author:
          type: object
          properties:
            id: { type: string }
            username: { type: string }
        upvotes: { type: integer }
        downvotes: { type: integer }
        score: { type: integer }
        comment_count: { type: integer }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time, nullable: true }
    Author:
      type: object
      properties:
        id: { type: string, example: "42" }
        username: { type: string, example: alex_92 }
    PostDetail:
      type: object
      properties:
        id: { type: string, example: "5021" }
        issue_id: { type: string, example: "100" }
        title: { type: string }
        content: { type: string, description: 마크다운 원문 }
        author: { $ref: "#/components/schemas/Author" }
        upvotes: { type: integer, example: 18 }
        downvotes: { type: integer, example: 2 }
        score: { type: integer, example: 16 }
        user_vote: { type: integer, enum: [1, -1], nullable: true, description: "로그인 시에만; 비로그인 null" }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time, nullable: true }
    Comment:
      type: object
      properties:
        id: { type: string, example: "7011" }
        post_id: { type: string, example: "5021" }
        parent_id: { type: string, nullable: true, description: "null=댓글, 값 있으면 대댓글" }
        depth: { type: integer, enum: [0, 1], description: "0=댓글, 1=대댓글" }
        content: { type: string }
        author: { $ref: "#/components/schemas/Author" }
        like_count: { type: integer, example: 12 }
        liked_by_me: { type: boolean, description: "로그인 시에만 true 가능" }
        created_at: { type: string, format: date-time }
    VoteResult:
      type: object
      properties:
        post_id: { type: integer, example: 5021 }
        value: { type: integer, enum: [1, -1], nullable: true, description: "취소 시 null" }
        action: { type: string, enum: [created, updated, deleted] }
    LikeResult:
      type: object
      properties:
        comment_id: { type: integer, example: 7011 }
        liked: { type: boolean }
        like_count: { type: integer, example: 13 }
        action: { type: string, enum: [created, deleted] }

  responses:
    BadInput:
      description: 입력값 오류 (BAD_INPUT)
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    Unauthorized:
      description: 인증 필요 (UNAUTHORIZED / INVALID_TOKEN)
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    Forbidden:
      description: 권한 없음 (FORBIDDEN — 관리자 전용)
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    NotFound:
      description: 리소스 없음 (NOT_FOUND / ISSUE_NOT_FOUND)
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
